import fs from "fs";
import { AeSdk, CompilerHttp, MemoryAccount, Node, } from "@aeternity/aepp-sdk";
import { readFile } from "fs/promises";
export const networks = JSON.parse(await readFile(new URL("./networks.json", import.meta.url)).then((file) => file.toString("utf-8")));
export const wallets = JSON.parse(await readFile(new URL("./wallets.json", import.meta.url)).then((file) => file.toString("utf-8")));
export function getContractContent(contractPath) {
    return fs.readFileSync(contractPath, "utf8");
}
export function getDefaultAccounts() {
    return wallets.map((keypair) => new MemoryAccount(keypair.secretKey));
}
export function getSdk(options) {
    const instance = new Node(networks.devmode.nodeUrl, options);
    return new AeSdk({
        accounts: getDefaultAccounts(),
        nodes: [{ name: "node", instance }],
        onCompiler: new CompilerHttp(networks.devmode.compilerUrl, options),
        interval: 50,
    });
}
export async function awaitKeyBlocks(aeSdk, n = 1) {
    const height = await aeSdk.getHeight();
    await fetch(`http://localhost:3001/emit_kb?n=${n}`);
    await aeSdk.awaitHeight(height + n);
}
let snapshotHeight = -1;
export async function createSnapshot(aeSdk) {
    snapshotHeight = await aeSdk.getHeight();
    await awaitKeyBlocks(aeSdk, 1);
}
export async function rollbackHeight(aeSdk, height) {
    const currentBlockHeight = await aeSdk.getHeight();
    if (currentBlockHeight > height) {
        await fetch(`http://localhost:3001/rollback?height=${height}`);
    }
}
export async function rollbackSnapshot(aeSdk) {
    if (snapshotHeight === -1)
        throw new Error("no snapshot created");
    return rollbackHeight(aeSdk, snapshotHeight);
}
//# sourceMappingURL=utils.js.map